#include "main.h"

/**
 * _strlen - prints the length of the variable
 * @s: the variable to be tested
 * Return: integer
 */
int _strlen(char *s)
{
	int x;

	for(i = 0; *s[i] != '\0'; i++)
    	{
        	x++; //Counting the length.
    	}

	Return x;
}
