#include <stdio.h>
#include "lists.h"

/**
 * dlistint_len - return the size of the list
 * @h: pointer to the start of the list
 * 
 * Return: the size of list
 */
size_t dlistint_len(const dlistint_t *h)
{
    int i;

    for (i = 0; h->n != NULL; i++)
    {

    }
    return (i);
}